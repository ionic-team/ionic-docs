export enum UsageTarget {
  Html = 'Basic',
  Angular = 'Angular',
  React = 'React',
  Vue = 'Vue',
}

export const UsageTargetList = Object.keys(UsageTarget);

export enum Mode {
  iOS = 'ios',
  MD = 'md',
}
